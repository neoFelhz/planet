language: node_js
dist: trusty
node_js:
  - "7"
install:
  - npm install
script:
  - \cp -f generate.js node_modules/planet.js/lib/generate.js
  - chmod +x ./deploy.sh
  - npm run build
  - \cp -f CNAME public/CNAME
  - ./deploy.sh > /dev/null
cache:
  directories:
    - node_modules
